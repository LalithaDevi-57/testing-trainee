<odoo>
    <record id="view_todo_task_tree" model="ir.ui.view">
        <field name="name">todo.task.tree</field>
        <field name="model">todo.task</field>
        <field name="arch" type="xml">
            <tree>
                <field name="name"/>
                <field name="due_date"/>
                <field name="is_done"/>
            </tree>
        </field>
    </record>

    <record id="view_todo_task_form" model="ir.ui.view">
        <field name="name">todo.task.form</field>
        <field name="model">todo.task</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="description"/>
                        <field name="due_date"/>
                        <field name="is_done"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="action_todo_task" model="ir.actions.act_window">
        <field name="name">ToDo Tasks</field>
        <field name="res_model">todo.task</field>
        <field name="view_mode">tree,form</field>
    </record>
     <record id="action_mark_done_wizard" model="ir.actions.act_window">
        <field name="name">Mark Tasks as Done</field>
        <field name="res_model">todo.mark.done.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>

    <menuitem id="menu_todo_root" name="ToDo" sequence="1"/>

    <menuitem id="menu_todo_task"
              name="Tasks"
              parent="menu_todo_root"
              action="action_todo_task"
              sequence="2"/>

   <menuitem id="menu_mark_done_wizard" name="Mark Tasks as Done"
             parent="menu_todo_root" action="action_mark_done_wizard" sequence="3"/>


</odoo>
